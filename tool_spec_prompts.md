# 🛠️ ツール完全技術仕様書 プロンプト集

> HTMLツールをAIに読み込ませて完全再現できるレベルの仕様書を作るためのプロンプト集。
> 用途に応じてフェーズを選んで使う。

---

## 目次

1. [フェーズ1：ファイル解析・構造把握](#フェーズ1ファイル解析構造把握)
2. [フェーズ2：仕様書の各セクション生成](#フェーズ2仕様書の各セクション生成)
3. [フェーズ3：仕様書まとめ・出力](#フェーズ3仕様書まとめ出力)
4. [フェーズ4：再現テスト用プロンプト](#フェーズ4再現テスト用プロンプト)
5. [応用：ツール種別ごとのプロンプト](#応用ツール種別ごとのプロンプト)
6. [チェックリスト](#チェックリスト)

---

## フェーズ1：ファイル解析・構造把握

### 1-1. 初期解析（ファイルを渡したとき最初に使う）

```
このHTMLファイルを一流エンジニアの視点でミクロ分析してください。

以下の順で出力してください：

## 1. 全体構造
- ファイルの種別（スタンドアロン／フラグメント／SPA等）
- 主要セクションの一覧（HTML構造ツリー）
- 外部依存（CDN・API・ライブラリ等）

## 2. データ構造
- 中心となるデータオブジェクトの完全スキーマ（全プロパティ・型・必須可否）
- データの初期化方法と格納場所

## 3. 状態管理
- 状態変数の一覧（名前・型・役割）
- 状態の変化フロー（どのイベントがどの変数を変えるか）

## 4. 関数一覧
- 全関数名とシグネチャ
- 呼び出し関係のマップ（どの関数がどれを呼ぶか）

## 5. スタイルシステム
- CSS変数の一覧と用途
- 主要コンポーネントのクラス名と役割
- テーマ依存（外部変数依存の有無）

出力は後でWord/Markdownに変換するため、表・コードブロックを積極的に使ってください。
```

---

### 1-2. 関数の詳細仕様抽出

```
このHTMLの全関数について、以下の形式で詳細仕様を出力してください。
1関数1ブロックで、漏れなく列挙すること。

### 関数名()
- **目的**：1文で
- **引数**：`引数名: 型` — 説明
- **戻り値**：型と内容
- **副作用**：DOM操作・状態変更・API呼び出し等
- **呼び出しタイミング**：どのイベント・関数から呼ばれるか
- **重要ロジック**：条件分岐・特殊処理のポイント
- **依存関係**：参照するグローバル変数・他関数

記述式より具体的に。例：「state["rev-N"]がtrueなら早期returnで以降の処理をスキップ」
```

---

### 1-3. CSSコンポーネント仕様抽出

```
このHTMLのCSSを解析し、以下の形式で全コンポーネントの仕様を出力してください。

## スタイルシステム概要
- テーマ方式（CSS変数・ハードコード・外部依存）
- カラーパレット（変数名・値・用途）
- フォント定義
- ブレークポイント（レスポンシブ対応の有無と閾値）

## コンポーネント一覧
各コンポーネントについて：

### .クラス名
- **要素種別**：（カード・ボタン・バッジ等）
- **通常状態**：key CSSプロパティ
- **ホバー/フォーカス状態**：変化するプロパティ
- **修飾クラス**：`.on` `.active` `.correct` 等のバリアントと意味
- **子要素との関係**：重要な子セレクタがあれば

色は16進数やvar()名で正確に記述してください。
```

---

### 1-4. データフロー解析

```
このHTMLのデータフローを以下の形式で分析してください。

## イベント→状態→DOM の変化マップ

| ユーザー操作 | 呼ばれる関数 | 変化する状態変数 | 更新されるDOM要素 |
|---|---|---|---|

## 状態変数の命名規則
- 各キーパターンの意味と値の型を説明してください
- 例：state["sel-N"] = number[] → 問題Nの選択中インデックス配列

## レンダリング戦略
- 全体再描画の条件と呼ばれる関数
- 差分更新（1要素更新）の条件と呼ばれる関数
- それぞれのパフォーマンス上の意図
```

---

## フェーズ2：仕様書の各セクション生成

### 2-1. 技術仕様書の概要セクション生成

```
このHTMLツールについて、「AIが読み込んで完全再現できる技術仕様書」の
概要セクションをMarkdownで書いてください。

含めるべき内容：
- ツールの目的・用途（1段落）
- 対象環境（ブラウザ・サーバー要否・フレームワーク）
- 技術スタック（表形式：要素・採用技術・理由）
- 外部依存の完全リスト（ライブラリ・API・フォント等）
- ファイル構成（スタンドアロンか分割か）

読み手はこのツールを「ゼロから再実装するAIエンジニア」と想定してください。
```

---

### 2-2. データ構造セクション生成（スキーマ定義）

```
このHTMLで使われているデータ構造を、AIが再実装できる精度で仕様化してください。

## データスキーマ仕様

各オブジェクト/配列について：
1. TypeScript風の型定義で示してください
2. 各プロパティに「用途・取りうる値・制約」を注釈してください
3. サンプルデータを3パターン（最小・通常・エッジケース）示してください
4. バリデーションルール（どの値が無効か）を列挙してください

## 状態管理オブジェクト仕様
- キーの命名規則（プレフィックスの意味を含む）
- 値の型と意味
- 初期値と初期化タイミング
- リセット時の挙動

コードブロックはJavaScriptで記述してください。
```

---

### 2-3. UI仕様セクション生成

```
このHTMLのUIを、デザイナーが見なくても再実装できる精度で仕様化してください。

## レイアウト仕様
- ページ全体の構造（Flexbox/Grid等の具体的な値）
- レスポンシブ対応（ブレークポイント・変化内容）

## 各コンポーネントの仕様
以下の形式で全コンポーネントを網羅：

### コンポーネント名
- **HTMLセマンティクス**：使用タグと意味
- **視覚仕様**：サイズ・色・ボーダー・パディング（具体的な値）
- **インタラクション**：ホバー・クリック・フォーカス時の変化
- **状態バリアント**：クラスによる状態変化（色・形）
- **子要素**：必須の子要素とその役割

数値は実際の値で（「大きめ」ではなく「16px」）。
```

---

### 2-4. ロジック仕様セクション生成

```
このHTMLのJavaScriptロジックを、コードなしで完全に再現できるレベルの
自然言語仕様として書いてください。

## アーキテクチャ概要
- グローバル変数とその役割
- 初期化フロー（ページロード時に何が起きるか）

## 機能ブロック別仕様

各機能について：
### [機能名]
**トリガー**：何が起点か（ユーザー操作・ページロード・他関数の呼び出し）
**前提条件**：実行前に満たすべき状態
**処理フロー**：1〜N. で順番に列挙（条件分岐は「〜の場合は〜、それ以外は〜」形式）
**結果**：状態変数・DOM・外部に何が起きるか
**エラーケース**：何が失敗しうるか・その処理

擬似コードではなく、日本語で正確に記述してください。
```

---

### 2-5. AI連携仕様セクション（API利用ツールの場合）

```
このHTMLのAI/API連携部分を完全仕様化してください。

## APIコール仕様

### エンドポイント
- URL・メソッド・ヘッダー

### リクエスト構造
- model・max_tokens等の固定パラメータ
- プロンプトの構造（テンプレート変数を[VAR_NAME]で明示）
- 動的に変わる部分と固定部分の区別

### レスポンス処理
- 期待するレスポンス形式
- パース処理（JSONブロック抽出・エラー処理の手順）
- 成功時・失敗時それぞれのUI更新

### プロンプトテンプレート（完全版）
プロンプト全文を実際のテンプレート変数込みで記載してください。
変数部分は {{変数名}} 形式で示してください。

### ログ・フィードバックUI
- ログ表示の仕様（形式・色・最大表示数等）
- ユーザーへのフィードバック（ローディング・成功・エラー）
```

---

## フェーズ3：仕様書まとめ・出力

### 3-1. 完全仕様書の統合出力（Markdown版）

```
これまでの分析を統合して、「AIが読んでHTMLツールを完全再現できる技術仕様書」を
以下の構成でMarkdownにまとめてください。

# [ツール名] 完全技術仕様書

## 1. 概要
## 2. ファイル構成・技術スタック
## 3. データ構造（型定義・スキーマ）
## 4. 状態管理（state変数の全仕様）
## 5. UIコンポーネント（全クラスの仕様）
## 6. スタイルシステム（CSS変数・テーマ）
## 7. 関数仕様（全関数の詳細）
## 8. イベント→状態→DOM変化マップ
## 9. 初期化フロー
## 10. エクスポート・インポート仕様（該当する場合）
## 11. AI連携仕様（該当する場合）
## 12. 付録：サンプルデータ・再現プロンプト

各セクションは「このセクションだけ読んでその部分を実装できる」密度で書いてください。
曖昧な表現（「適切に」「大きめに」等）は使わないこと。
```

---

### 3-2. AIへの再現指示プロンプト生成（仕様書末尾に添付）

```
この仕様書を使ってAIがツールを再現するための「再現指示プロンプト」を
仕様書の付録として書いてください。

以下の3バリアントを作成してください：

### バリアントA：完全再現（全機能）
仕様書を読み込んだAIへの指示文。
「この仕様書に基づき〜を実装してください。出力形式は〜」の形式。

### バリアントB：特定機能のみ再現
「プレイヤー部分のみ」「AI生成タブのみ」等、特定機能を切り出す指示文。

### バリアントC：別トピックへの流用
「同じ構造で問題をXXX（別テーマ）に変えて作成してください」の形式。

各バリアントに：
- どの出力形式にするか（Claude Artifacts向け / スタンドアロンHTML / フラグメント）
- 何問・何カテゴリ等の具体的な数値指定
- 注意事項（トークン制限対策・分割出力のコツ等）
を含めてください。
```

---

### 3-3. Word仕様書生成指示（Node.js/docxライブラリ）

```
この技術仕様書をWordファイル（.docx）として生成するNode.jsコードを書いてください。

要件：
- ライブラリ：docx（npm install -g docx）
- フォント：Arial（日本語環境でも安全）
- ページサイズ：A4（11906 × 16838 DXA）
- マージン：上下左右 1080 DXA（約19mm）

含める内容：
1. カバーページ（タイトル・サブタイトル・日付）
2. 目次（見出しレベル1〜2）
3. 各セクション（h1/h2/h3の階層構造）
4. 表（テーブル）：ヘッダー行は濃色背景＋白文字、偶数行は薄いグレー背景
5. コードブロック：等幅フォント・薄グレー背景・左ボーダーアクセント
6. 注記ボックス：💡ポイント・⚠️注意・✅推奨をインラインスタイルで

Wordで開けることを最優先。フォントはNoto Sans JPを使わず、Arialのみ使用。
テーブルはWidthType.DXAで固定幅。columnWidthsとセルwidthを両方設定。
```

---

## フェーズ4：再現テスト用プロンプト

### 4-1. 仕様書を渡してツールを再現させる

```
添付の技術仕様書に基づき、HTMLツールを完全に再現してください。

【出力形式】
Claude Artifacts向けのHTMLフラグメント（<html>/<head>/<body>タグなし）

【再現の優先順位】
1. データ構造（QSオブジェクトのスキーマを正確に）
2. 状態管理ロジック（stateオブジェクトのキー命名規則）
3. レンダリング関数（buildCard/refreshCard等）
4. スタイル（Claude.ai CSS変数を使用）

【問題データについて】
仕様書のサンプルデータ3問のみ含めてください（動作確認用）。
問題を増やすのは後の指示で行います。

【分割出力が必要な場合】
- Part 1：HTML構造＋CSS
- Part 2：データ＋JavaScript
として分割してください。
```

---

### 4-2. 問題データを追加する（再現後に使う）

```
先ほど作成したHTMLツールのQS配列に、以下の問題を追加してください。

追加する問題のスキーマ（仕様書参照）：
{
  n: [番号],
  cat: "[カテゴリ]",
  text: "[問題文]",
  type: "single" | "multi" | "input",
  opts: ["選択肢A", "選択肢B", ...],  // input型は[]
  ans: [正解インデックス],             // input型は["正解文字列"]
  explain: "[解説HTML]"
}

追加問題：
[ここに問題内容を箇条書きで]

既存のコードは変更せず、QS = [...] の配列部分だけ差し替えてください。
```

---

### 4-3. スタンドアロンHTML変換

```
このHTMLフラグメント（Claude.ai Artifacts形式）を、
ブラウザでダブルクリックして開けるスタンドアロンHTMLに変換してください。

変換の要件：
1. <!DOCTYPE html><html lang="ja">... の完全な構造に変える
2. Claude.ai CSS変数をすべて:rootで定義する
   （ライト/ダークモード両対応。@media prefers-color-schemeで切り替え）
3. 外部依存がある場合はCDNリンクを追加
4. <meta charset="UTF-8">と<meta name="viewport">を追加
5. <title>タグを適切に設定

CSS変数の定義例（ライトモード）：
:root {
  --color-text-primary: #1a1a2e;
  --color-background-primary: #ffffff;
  --color-background-secondary: #f5f6fa;
  --color-text-info: #1d6fa4;
  --color-background-info: #e6f0fa;
  --color-text-success: #1a7a3c;
  --color-background-success: #e6f5ec;
  --color-text-danger: #9a2020;
  --color-background-danger: #fde8e8;
  --color-border-tertiary: #e0e0e8;
  --color-border-secondary: #cccccc;
  --border-radius-md: 6px;
  --border-radius-lg: 10px;
  --font-sans: system-ui, sans-serif;
  --font-mono: Consolas, monospace;
}
```

---

## 応用：ツール種別ごとのプロンプト

### A. クイズ・学習ツール系

```
このクイズHTMLツールの「問題データ設計仕様」に特化して分析してください。

1. **問題オブジェクトの完全スキーマ**（全フィールドの型・制約・例）
2. **問題形式ごとの仕様**
   - 単一選択：選択肢数の制約・正解判定ロジック
   - 複数選択：選択数の制約・ソートが必要な理由
   - 記述式：正規化処理（trim/toLowerCase等）・複数正解の扱い
3. **解説HTMLの書式ガイド**（使えるタグ・クラス・絵文字の慣例）
4. **カテゴリシステム**（cat値・表示名・バッジ色の対応表）
5. **別試験に流用するときの変更箇所チェックリスト**
```

---

### B. フォーム・入力ツール系

```
このHTMLフォームツールについて以下を仕様化してください。

1. **フォームフィールド仕様**
   | フィールドID | 型 | デフォルト値 | バリデーション | エラーメッセージ |

2. **バリデーションロジック**
   - どの順序でバリデーションが走るか
   - 全フィールドの検証後か・フィールドごとのリアルタイムか

3. **フォームの状態遷移**
   - 初期状態 → 入力中 → 送信 → 成功/失敗

4. **送信処理**
   - fetch/XHRの詳細（URL・メソッド・ヘッダー・ボディ構造）
   - 成功・失敗・ネットワークエラーそれぞれの処理

5. **UI動的変化**
   - どのフィールドが他のフィールドの値によって表示/非表示になるか
```

---

### C. ダッシュボード・可視化ツール系

```
このHTMLダッシュボードツールのデータ可視化仕様を分析してください。

1. **チャート/グラフの種類と設定**
   - ライブラリ（Chart.js・D3等）のバージョンと使用API
   - 各チャートの設定オブジェクト（options・data構造）

2. **データパイプライン**
   - 生データ → 表示データへの変換処理
   - フィルター・集計・ソートのロジック

3. **インタラクション仕様**
   - クリック・ホバー・ドラッグで何が起きるか
   - チャート間の連動（クロスフィルタ等）

4. **データ更新仕様**
   - ポーリング間隔・WebSocket等のリアルタイム更新
   - アニメーション設定

5. **レスポンシブ対応**
   - ブレークポイントでのレイアウト変化
   - チャートのリサイズ処理
```

---

### D. マルチタブ・SPA系

```
このマルチタブHTMLツールのタブシステムを完全仕様化してください。

1. **タブ管理アーキテクチャ**
   - タブの識別方法（ID・インデックス・名前）
   - アクティブタブの状態保持方法
   - タブ切り替え時に何が起きるか（DOM操作・データロード・アニメーション）

2. **パネル間のデータ共有**
   - どのデータがパネル間で共有されるか
   - パネルAで変更したデータがパネルBに反映されるタイミング

3. **各タブの初期化処理**
   - ページロード時に初期化されるタブ
   - タブを開いたときに初めて初期化されるタブ（遅延初期化）

4. **タブ間のナビゲーション**
   - プログラムからのタブ切り替え（switchTab()等）
   - どの操作がどのタブへ遷移させるか

5. **状態の分離**
   - タブごとに独立した状態変数
   - 全タブで共有する状態変数
```

---

## チェックリスト

### 仕様書の品質チェック

仕様書を作ったら、以下の問いに答えられるか確認してください。

```
この仕様書を使ってHTMLツールを再現するとき、
以下の問いに答えられますか？

□ データオブジェクトの全フィールドと型が分かるか？
□ 状態変数のキー命名規則が分かるか？
□ 全関数の引数・戻り値・副作用が分かるか？
□ どの操作がどの関数を呼ぶか分かるか？
□ CSSクラスの全状態バリアント（.on .active等）が分かるか？
□ 色は具体的な値（#HEXまたはvar(--名前)）で記載されているか？
□ APIコールのリクエスト/レスポンス構造が分かるか？
□ エラーケースの処理が記載されているか？
□ ページロード時の初期化フローが分かるか？

すべてYESなら「完全技術仕様書」として合格。
NOがあれば対応するフェーズのプロンプトを再実行。
```

---

### プロンプト選択ガイド

| やりたいこと | 使うプロンプト |
|---|---|
| ツールの全体像を素早く把握したい | 1-1 |
| 関数の詳細をすべて抽出したい | 1-2 |
| CSSコンポーネントを細かく把握したい | 1-3 |
| データの流れを理解したい | 1-4 |
| Markdown仕様書を一気に作りたい | 3-1 |
| AI再現用プロンプトを作りたい | 3-2 |
| Wordファイルで仕様書を作りたい | 3-3 |
| 仕様書からツールを再現させたい | 4-1 |
| 問題/データを追加したい | 4-2 |
| スタンドアロンHTMLに変換したい | 4-3 |
| クイズ系ツールの問題設計を仕様化 | A |
| フォーム系ツールを仕様化 | B |
| ダッシュボード系を仕様化 | C |
| マルチタブSPAを仕様化 | D |

---

*作成日：2026-06-12 / 対象：HTMLツールの完全技術仕様書作成ワークフロー*
