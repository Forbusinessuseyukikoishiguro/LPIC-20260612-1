# 🐧 家で楽しくLPICを突破しよう！新卒エンジニア向け模試ツール完全ガイド

> **「テキストを3周したのに本番で手が止まった…」**  
> そんな経験をなくすために作った、全60問・即時フィードバック付きのインタラクティブ学習ツールを紹介します。

---

## なぜ「読むだけ」では合格できないのか

LPIC-1（101試験）の合格率が低い最大の理由は、**コマンドを「知っている」と「使える」の混同**です。

テキストで `find` の書き方を読んでいても、試験の選択肢を目の前にすると急に自信がなくなる。それは知識ではなく、**正しい選択肢を選ぶ判断力**が鍛えられていないサインです。

このツールは「選択肢を見て考える」という試験本番と同じ認知プロセスを、自宅で何度でも繰り返すために作りました。

---

## ツールの特徴

### ⚡ 即時フィードバック
「回答を確認」を押した瞬間、正解・不正解と**詳細な解説が展開**されます。記憶の定着には、答え合わせのタイミングが極めて重要です。

### 🎯 全60問・3形式対応
単一選択・複数選択・**記述式**の3形式を網羅。本番試験と同じ形式で思考力を鍛えられます。

### 🗂️ 7カテゴリで弱点特訓
カテゴリボタンをワンクリックするだけで、苦手分野だけを集中して解けます。

### 📊 進捗バーでモチベ維持
正解数・不正解数・回答済み数をリアルタイム表示。「あと何問」が見えると最後まで続けられます。

### 🔄 何度でもリセット可能
「最初からやり直す」ボタンで状態を完全リセット。間違えた問題を繰り返し解くことで着実に定着します。

---

## 7カテゴリ完全対応

| カテゴリ | 主な出題内容 | こんな人が苦手 |
|----------|-------------|---------------|
| **コマンド基礎** | find / locate / tar / cut / which / type | Windowsしか使ったことがない方 |
| **vi エディタ** | モード切替・削除・コピペ・保存・終了 | VSCodeに慣れきった方 😅 |
| **正規表現・grep** | 文字クラス・量詞・grep -E | 記号の意味が全部同じに見える方 |
| **リダイレクト** | stdout/stderr・パイプ・tee・`2>&1` | `2>&1` の意味が謎な方 |
| **パーミッション** | chmod（数値・記号）・SUID/SGID・umask | 755と644の違いが曖昧な方 |
| **プロセス管理** | ps / kill / nice / bg / fg / jobs | 「プロセスって何？」な方 |
| **ユーザー管理** | useradd / passwd / /etc/passwd | sudoとsuの違いが怪しい方 |

---

## 合格者が実践した5つの使い方

### 1. まず全問ざっと解く
解説を読む前に直感で全60問を解き切りましょう。どのカテゴリが弱いか可視化してから対策すると効率が上がります。

### 2. 解説のコードを実際に打つ
解説に出てきたコマンドは**必ずターミナルで実行**してください。

```bash
$ find /tmp -user root -print
$ chmod 755 script.sh
$ grep -E ':08:[0-9]+:[0-9]+' access.log
```

見た目で覚えるより、手が覚えるほうが本番でミスしません。

### 3. 不正解カードに集中する
赤いボーダーのカード（不正解）だけフィルタして集中復習。正解できた問題を何度も解くのは時間の無駄です。

### 4. 記述式は大文字に注意

```
updatedb  ← ✅ 正解
UpdateDB  ← ❌ 不正解
```

コマンドはすべて**小文字**と体で覚えましょう。

### 5. 通勤・昼休みに1カテゴリ
カテゴリ絞り込みで1セッション8〜10問程度になります。スマホでも十分読めるので、スキマ時間を活用してください。

---

## 💡 解説の読み方

解説内に登場する2つのマークは特に重要です。

- 💡 **試験頻出** — 本番でほぼ確実に出るポイント。ここだけ読み返すだけで正答率が変わります
- ⚠️ **注意** — ひっかけられやすいポイント。「知っているつもり」の落とし穴です

---

## 3週間合格スケジュール（1日30〜45分）

```
WEEK 1前半  コマンド基礎 ＋ リダイレクト
            └ find / tar / cut と 2>&1 の仕組みを完全理解

WEEK 1後半  vi エディタ
            └ dd / yy / p / :wq / :q! を実機で練習しながら解く

WEEK 2      正規表現 ＋ パーミッション
            └ 最も落とされる2カテゴリを集中攻略
            └ chmod の数値計算（4=r / 2=w / 1=x）を叩き込む

WEEK 3前半  プロセス管理 ＋ ユーザー管理
            └ 残りカテゴリを仕上げる

WEEK 3後半  全問通し復習
            └ 正解率85%超えで合格圏内
            └ 不正解が続くカテゴリを再フィルタして最終仕上げ

前日        新しい勉強はせず 💡⚠️マークの解説だけ読み返す
```

---

## ツールスペック

| 項目 | 内容 |
|------|------|
| 総問題数 | 60問 |
| カテゴリ数 | 7カテゴリ |
| 問題形式 | 単一選択・複数選択・記述式 |
| 詳細解説 | 全60問分 |
| 環境 | ブラウザのみ（インストール不要） |
| 対応デバイス | PC / スマートフォン |

---

## さいごに

コマンドの意味は知っているのに選択肢を選べない、という感覚。それは**知識ではなく判断力が足りていないサイン**です。

模試を繰り返すことで、考え方のパターンが自然と身につきます。テキストと並行してこのツールを使い、**「分かる」から「選べる」へ**ステップアップしてください。

新卒エンジニアのみなさんの合格を応援しています 🐧

---

*LPIC-1 101 試験対策 全60問 インタラクティブ学習ツール — 新卒研修エンジニア向け紹介記事*
